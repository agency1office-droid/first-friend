"use client";import{useState}from"react";import{ActionButton}from"seed-design/ui/action-button";import{TextField,TextFieldTextarea}from"seed-design/ui/text-field";import{Callout}from"seed-design/ui/callout";
export function ApplicationForm({animalId,animalName}:{animalId:string;animalName:string}){const[sent,setSent]=useState(false),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),body={animalId,household:String(f.get("household")||""),carePlan:String(f.get("carePlan")||""),readinessScore:78};const r=await fetch("/api/applications",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(r.ok)setSent(true);else if(r.status===401)window.location.href=`/signin-with-chatgpt?return_to=${encodeURIComponent(`/apply/${animalId}`)}`;else setError("신청을 저장하지 못했어요.")}if(sent)return <div className="ff-result"><h2 className="ff-section-title">{animalName}의 보호자에게 마음이 전달됐어요</h2><p className="ff-description" style={{margin:"8px 0 16px"}}>모든 신청자는 빠짐없이 확인되며 상담 순서를 안내해 드려요.</p><ActionButton asChild variant="neutralSolid"><a href="/mypage">진행 상황 보기</a></ActionButton></div>;return <form className="ff-form" onSubmit={submit}><TextField label="함께 사는 환경" description="주거 형태, 동거인, 기존 반려동물, 평일 생활 리듬을 적어주세요." required><TextFieldTextarea name="household" required minLength={20}/></TextField><TextField label="돌봄 계획" description="산책·놀이, 장기 부재, 병원과 비상 비용 계획을 적어주세요." required><TextFieldTextarea name="carePlan" required minLength={20}/></TextField><Callout tone="informative" description="준비도는 상담 참고 정보이며 최종 승인은 보호자가 결정해요."/>{error&&<p role="alert">{error}</p>}<ActionButton size="large" className="ff-action-link">본인 확인 후 신청하기</ActionButton></form>}
