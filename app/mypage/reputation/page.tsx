import type{Metadata}from"next";import{eq}from"drizzle-orm";import{requireChatGPTUser}from"../../chatgpt-auth";import{getDb}from"../../../db";import{drawingMatches,volunteerBadges}from"../../../db/schema";import{Badge}from"@seed-design/react";export const dynamic="force-dynamic";export const metadata:Metadata={title:"나의 포인트와 봉사 배지"};export default async function Page(){const user=await requireChatGPTUser("/mypage/reputation"),db=getDb(),matches=await db.select().from(drawingMatches).where(eq(drawingMatches.memberId,user.userId)),badges=await db.select().from(volunteerBadges).where(eq(volunteerBadges.memberId,user.userId)),points=matches.reduce((sum,row)=>sum+row.points,0),grade=points>=1000?"퍼스트 프렌드 탐정":points>=300?"매칭 메이트":points>=100?"따뜻한 관찰자":"새싹 친구";return <div className="ff-page"><header className="ff-page-header"><div className="ff-kicker">행동으로 쌓은 신뢰</div><h1 className="ff-title">{grade}</h1><p className="ff-description">그림 매칭 채택과 보호소가 완료 확인한 봉사만 포인트·배지에 반영합니다.</p></header><div className="ff-reputation-score"><strong>{points}P</strong><span>매칭 도움 포인트</span></div><section className="ff-section"><h2 className="ff-section-title">나의 봉사 배지</h2><div className="ff-badge-wall">{badges.map(row=><div key={row.id}><span>🏅</span><Badge tone="positive" variant="weak">{row.label}</Badge></div>)}</div>{!badges.length&&<div className="ff-empty">첫 봉사를 완료하면 보호소 확인 후 배지가 생겨요.</div>}</section></div>}
