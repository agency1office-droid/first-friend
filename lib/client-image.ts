"use client";
// 브라우저에서 다시 인코딩해 EXIF 위치·기기 메타데이터를 제거하고 과도한 해상도를 줄입니다.
export async function sanitizeImageFile(file:File,maxDimension=2000){if(!file.type.startsWith("image/"))return file;const bitmap=await createImageBitmap(file),scale=Math.min(1,maxDimension/Math.max(bitmap.width,bitmap.height)),canvas=document.createElement("canvas");canvas.width=Math.max(1,Math.round(bitmap.width*scale));canvas.height=Math.max(1,Math.round(bitmap.height*scale));const context=canvas.getContext("2d");if(!context){bitmap.close();return file;}context.fillStyle="#fff";context.fillRect(0,0,canvas.width,canvas.height);context.drawImage(bitmap,0,0,canvas.width,canvas.height);bitmap.close();const type=file.type==="image/png"?"image/png":"image/webp",blob=await new Promise<Blob|null>(resolve=>canvas.toBlob(resolve,type,.88));return blob?new File([blob],file.name.replace(/\.[^.]+$/,type==="image/png"?".png":".webp"),{type,lastModified:Date.now()}):file;}
