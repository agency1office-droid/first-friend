import type{Metadata}from"next";import{desc,eq}from"drizzle-orm";import{requireChatGPTUser}from"../../chatgpt-auth";import{getDb}from"../../../db";import{applications}from"../../../db/schema";import{List,ListDivider,ListLinkItem}from"seed-design/ui/list";import{IconChevronRightLine,IconQUppercaseChatbubbleRightLine}from"@karrotmarket/react-monochrome-icon";export const dynamic="force-dynamic";export const metadata:Metadata={title:"입양 상담 메시지"};export default async function Page(){const user=await requireChatGPTUser("/mypage/messages"),rows=await getDb().select().from(applications).where(eq(applications.memberId,user.userId)).orderBy(desc(applications.createdAt));return <div className="ff-page"><header className="ff-page-header"><div className="ff-kicker">연락처를 공개하지 않고</div><h1 className="ff-title">입양 상담 메시지</h1><p className="ff-description">각 신청의 상담·동의·인계 기록을 한 흐름에서 확인해요.</p></header><List>{rows.map((row,index)=><div key={row.id}><ListLinkItem href={`/applications/${row.id}`} prefix={<IconQUppercaseChatbubbleRightLine/>} title={`친구 ${row.animalId} 상담`} detail={`${row.status} · 적합도 ${row.suitabilityScore}점`} suffix={<IconChevronRightLine/>}/>{index<rows.length-1&&<ListDivider/>}</div>)}</List>{!rows.length&&<div className="ff-empty">진행 중인 입양 상담이 없어요.</div>}</div>}
