"use client";import{ActionButton}from"seed-design/ui/action-button";import{TextField,TextFieldInput,TextFieldTextarea}from"seed-design/ui/text-field";import{Callout}from"seed-design/ui/callout";import{useState}from"react";import{useAppFeedback}from"./AppFeedback";
export function ShelterFundraiserForm(){const[error,setError]=useState(""),feedback=useAppFeedback();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/community",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"fundraiser-create",...Object.fromEntries(f.entries())})}),b=await r.json();if(r.ok){e.currentTarget.reset();setError("");feedback.success("검증 모금 심사를 요청했어요")}else setError(b.error)}return <section className="ff-result"><h2 className="ff-section-title">동물별 투명 모금 신청</h2><Callout tone="warning" description="치료 견적·사용 목적·목표금액을 검토한 뒤 공개됩니다. 일반 운영비와 섞을 수 없고 종료 후 사용 결과를 공개해야 합니다."/><form className="ff-form" onSubmit={submit}><TextField label="보호동물 공고번호"><TextFieldInput name="animalId" required/></TextField><TextField label="캠페인 제목"><TextFieldInput name="title" minLength={5} required/></TextField><TextField label="치료·간식·환경개선 목적과 산정 근거"><TextFieldTextarea name="purpose" minLength={30} required/></TextField><TextField label="목표금액"><TextFieldInput name="targetAmount" type="number" min="10000" required/></TextField>{error&&<Callout tone="critical" description={error}/>}<ActionButton>운영자 검토 요청</ActionButton></form></section>}
