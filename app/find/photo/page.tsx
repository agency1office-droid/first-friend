import type { Metadata } from "next";import { Finder } from "../../components/Finder";import { getAnimalsWithPhotoCounts } from "../../../lib/public-data";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"사진으로 친구 찾기"};export default async function Page(){return <div className="ff-page"><header className="ff-page-header"><div className="ff-kicker">사진 검색</div><h1 className="ff-title">닮은 모습을 사진으로 찾아요</h1><p className="ff-description">업로드한 사진은 기기에서 특징만 분석하며 서버에 저장하지 않아요.</p></header><Finder animals={await getAnimalsWithPhotoCounts(30)} modeOnly="photo"/></div>}
