import { getChatGPTUser } from "../../chatgpt-auth";import { getSupabaseServerClient } from "../../../lib/supabase/server";import { clean } from "../_helpers";
export async function GET(){const user=await getChatGPTUser();if(!user)return Response.json({homeRegion:""});const{data}=await getSupabaseServerClient().from("members").select("home_region").eq("id",user.userId).limit(1);return Response.json({homeRegion:data?.[0]?.home_region||""});}
export async function POST(request:Request){const user=await getChatGPTUser();if(!user)return Response.json({error:"로그인이 필요합니다."},{status:401});const data=await request.json()as Record<string,unknown>,homeRegion=clean(data.homeRegion,80).split(/\s+/).slice(0,3).join(" ");if(homeRegion.length<2)return Response.json({error:"시·군·구 수준의 지역을 입력해 주세요."},{status:400});await getSupabaseServerClient().from("members").update({home_region:homeRegion}).eq("id",user.userId);return Response.json({homeRegion});}
