"use client";import{useState}from"react";import{ActionButton}from"seed-design/ui/action-button";import{Callout}from"seed-design/ui/callout";export function SupportIntentButton({kind,title,targetId,amount=0,label}:{kind:"operations"|"goods"|"shelter_item"|"affiliate"|"insurance_referral";title:string;targetId:string;amount?:number;label:string}){const[message,setMessage]=useState("");async function act(){const r=await fetch("/api/support",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind,title,targetId,amount})});if(r.status===401){location.href=`/signin-with-chatgpt?return_to=${encodeURIComponent(location.pathname)}`;return}const b=await r.json();setMessage(b.message||b.error)}return <div><ActionButton onClick={act}>{label}</ActionButton>{message&&<Callout tone="informative" description={message}/>}</div>}
