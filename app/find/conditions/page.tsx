import type { Metadata } from "next";import { Finder } from "../../components/Finder";import { getAnimalsWithPhotoCounts } from "../../../lib/public-data";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"조건으로 친구 찾기"};export default async function Page(){return <div className="ff-page"><header className="ff-page-header ff-subpage-header"><a href="/find">‹ 친구 찾기</a><div className="ff-kicker">조건 검색</div><h1 className="ff-title">함께 살 모습을 골라보세요</h1><p className="ff-description">품종·털색·나이·성별·지역 중 한 가지만 골라도 괜찮아요. 넓게 찾아보고 생활 조건은 상세 화면에서 확인해요.</p></header><Finder animals={await getAnimalsWithPhotoCounts(30)} modeOnly="conditions"/></div>}
