"use client"; import {useState} from "react";
export function LostFoundForm(){const[kind,setKind]=useState("lost");const[done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch("/api/lost-found",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind,species:f.get("species"),region:f.get("region"),occurredAt:f.get("occurredAt"),description:f.get("description")})});if(res.status===401){window.location.href="/signin-with-chatgpt?return_to=%2Flost-found";return}if(res.ok)setDone(true)}if(done)return <div className="result-card"><h2>{kind==="lost"?"실종 신고":"발견 제보"}를 안전하게 접수했어요.</h2><p>정확한 위치와 연락처는 공개되지 않으며, 가능성 높은 제보가 생기면 본인 확인 후 연결합니다.</p></div>;return <form className="form-card" onSubmit={submit}><div className="radio-row" style={{marginBottom:18}}><label className="radio-card"><input type="radio" checked={kind==="lost"} onChange={()=>setKind("lost")}/> 반려동물을 찾고 있어요</label><label className="radio-card"><input type="radio" checked={kind==="found"} onChange={()=>setKind("found")}/> 동물을 발견했어요</label></div><div className="field"><label htmlFor="species">동물 종류</label><select name="species" id="species"><option>고양이</option><option>강아지</option><option>새</option><option>기타</option></select></div><div className="field"><label htmlFor="region">대략적인 지역</label><input id="region" name="region" placeholder="예: 서울 마포구" required/></div><div className="field"><label htmlFor="occurredAt">마지막 목격 또는 발견 시각</label><input id="occurredAt" name="occurredAt" type="datetime-local" required/></div><div className="field"><label htmlFor="description">외형과 상황</label><textarea id="description" name="description" required minLength={20} placeholder="털색, 무늬, 목걸이, 이동 방향 등 확인한 사실만 적어주세요."/></div><div className="notice">발견한 동물을 무리하게 쫓거나 포획하지 마세요. 도로 등 위험한 상황이면 안전한 거리에서 관계기관에 먼저 도움을 요청하세요.</div><button className="primary-button" style={{width:"100%"}}>본인 확인 후 안전하게 등록</button></form>}
