import type {Metadata} from "next";import {Finder} from "../components/Finder";import{getAnimalsWithPhotoCounts}from"../../lib/public-data";export const dynamic="force-dynamic";export const metadata:Metadata={title:"친구 찾기"};export default async function FindPage(){const animals=await getAnimalsWithPhotoCounts(30);return <div className="ff-page"><header className="ff-page-header"><div className="ff-kicker">공공데이터 실시간 연동</div><h1 className="ff-title">나와 닮은 친구를<br/>찾아보세요</h1><p className="ff-description">국가동물보호정보시스템에서 가족을 기다리는 친구를 불러옵니다. AI는 외형의 유사성만 비교하며 건강·성격·입양 성공을 판단하지 않아요.</p></header><Finder animals={animals}/></div>}
