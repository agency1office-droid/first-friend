"use client";import{useState}from"react";import{ActionButton}from"seed-design/ui/action-button";import{TextField,TextFieldInput,TextFieldTextarea}from"seed-design/ui/text-field";import{Callout}from"seed-design/ui/callout";
export function PostForm(){const[done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/posts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({category:f.get("category"),title:f.get("title"),body:f.get("body")})});if(r.status===401){window.location.href="/signin-with-chatgpt?return_to=%2Fstories%2Fnew";return}if(r.ok)setDone(true)}if(done)return <div className="ff-result"><h2 className="ff-section-title">이야기를 공개했어요</h2><p className="ff-description" style={{margin:"8px 0 16px"}}>당신의 기록이 다음 가족에게 따뜻한 용기가 됩니다.</p><ActionButton asChild variant="neutralSolid"><a href="/stories">이야기 보기</a></ActionButton></div>;return <form className="ff-form" onSubmit={submit}><Callout tone="warning" title="전체 공개 안내" description="글은 검색될 수 있어요. 정확한 위치, 급식 장소, 연락처는 적지 말아주세요."/><div className="ff-field"><label htmlFor="category">이야기 종류</label><select className="ff-native-select" id="category" name="category"><option value="memory">오늘의 추억</option><option value="neighborhood">동네 친구</option><option value="adoption">입양 일기</option><option value="rescue">보호 이야기</option></select></div><TextField label="제목" required maxGraphemeCount={80}><TextFieldInput name="title" required/></TextField><TextField label="이야기" required maxGraphemeCount={4000}><TextFieldTextarea name="body" required minLength={20}/></TextField><ActionButton size="large" className="ff-action-link">본인 확인 후 공개하기</ActionButton></form>}
