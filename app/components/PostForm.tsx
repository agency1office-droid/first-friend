"use client"; import {useState} from "react";
export function PostForm(){const[done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const res=await fetch("/api/posts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({category:form.get("category"),title:form.get("title"),body:form.get("body")})});if(res.status===401){window.location.href="/signin-with-chatgpt?return_to=%2Fstories%2Fnew";return}if(res.ok)setDone(true)}if(done)return <div className="result-card"><h2>이야기를 공개했어요.</h2><p>당신의 기록이 다음 가족에게 따뜻한 용기가 됩니다.</p><a className="secondary-button" href="/stories">이야기 보러 가기</a></div>;return <form className="form-card" onSubmit={submit}><div className="notice">이 글은 전체 공개되고 검색될 수 있어요. 정확한 위치, 급식 장소, 연락처는 적지 말아주세요.</div><div className="field"><label htmlFor="category">이야기 종류</label><select id="category" name="category"><option value="memory">오늘의 추억</option><option value="neighborhood">동네 친구</option><option value="adoption">입양 일기</option><option value="rescue">보호 이야기</option></select></div><div className="field"><label htmlFor="title">제목</label><input id="title" name="title" required maxLength={80}/></div><div className="field"><label htmlFor="body">이야기</label><textarea id="body" name="body" required minLength={20} maxLength={4000}/></div><button className="primary-button" style={{width:"100%"}}>본인 확인 후 공개하기</button></form>}
