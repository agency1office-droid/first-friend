"use client";import{ActionButton}from"seed-design/ui/action-button";import{useAppFeedback}from"./AppFeedback";export function VolunteerButton({shelterId,shelterName,region,postId,demo=false}:{shelterId:string;shelterName:string;region:string;postId:number;demo?:boolean}){const feedback=useAppFeedback();async function apply(){if(demo){feedback.success("봉사 지원 화면을 확인했어요");return}const response=await fetch("/api/volunteer",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({shelterId,shelterName,region,postId,message:"안전교육과 일정 안내를 받고 싶어요."})});if(response.status===401){location.href=`/login?return_to=${encodeURIComponent(location.pathname)}`;return}const body=await response.json();if(response.ok)feedback.success("봉사 지원을 접수했어요");else feedback.error(body.error||"봉사 지원을 접수하지 못했어요")}return <ActionButton size="small" onClick={apply}>지원</ActionButton>}
