import type { Metadata } from "next";import { Finder } from "../../components/Finder";import { getAnimalsWithPhotoCounts } from "../../../lib/public-data";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"그림으로 친구 찾기"};export default async function Page(){return <div className="ff-page"><header className="ff-page-header"><div className="ff-kicker">그림 검색</div><h1 className="ff-title">마음속 친구를 그려보세요</h1><p className="ff-description">색과 모양을 검색 태그로 바꿔 실제 보호동물과 비교해요.</p></header><Finder animals={await getAnimalsWithPhotoCounts(30)} modeOnly="draw"/></div>}
