import type{Metadata}from"next";import{stories}from"../../lib/data";import{StoryCard}from"../components/StoryCard";import{ActionButton}from"seed-design/ui/action-button";export const metadata:Metadata={title:"이야기"};export default function StoriesPage(){return <div className="ff-page"><header className="ff-page-header"><div className="ff-kicker">이야기</div><h1 className="ff-title">함께 살아가는<br/>마음의 기록</h1><p className="ff-description">댓글과 별점 대신 조용한 공감과 응원을 나눠요. 모든 이야기는 공개됩니다.</p></header><div className="ff-filter-bar">{["전체","입양 일기","동네 친구","오늘의 추억","보호 이야기"].map((x,i)=><button className="ff-filter-button" data-active={i===0} key={x}>{x}</button>)}</div><div className="ff-story-list">{stories.map(s=><StoryCard story={s} key={s.id}/>)}</div><div style={{marginTop:24}}><ActionButton asChild size="large" className="ff-action-link"><a href="/stories/new">나의 이야기 쓰기</a></ActionButton></div></div>}
