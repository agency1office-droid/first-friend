import type {Metadata} from "next"; import {stories} from "../../lib/data"; import {StoryCard} from "../components/StoryCard";
export const metadata:Metadata={title:"이야기",description:"동물과 사람이 함께 살아가는 공개 이야기"};
export default function StoriesPage(){return <div className="page"><span className="eyebrow">Stories we share</span><h1 className="page-title">함께 살아가는<br />마음의 기록</h1><p className="page-subtitle">댓글과 별점 대신 조용한 공감과 응원을 나눕니다. 모든 이야기는 공개되며 정확한 위치와 연락처는 보호합니다.</p><div className="filter-row">{["전체","입양 일기","동네 친구","오늘의 추억","보호 이야기"].map((x,i)=><span className={`filter-chip ${i===0?"active":""}`} key={x}>{x}</span>)}</div><div style={{display:"grid",gap:14}}>{stories.map((story)=><StoryCard story={story} key={story.id}/>)}</div><a className="primary-button" style={{width:"100%",marginTop:18}} href="/stories/new">나의 이야기 쓰기</a></div>}
