# 퍼스트프렌드 작업 규칙

이 저장소의 모든 작업은 이 문서를 먼저 읽고 따른다. 다른 채팅창이나 작업 세션에서 시작한 작업도 같은 기준을 적용한다.

## 범위와 배포

- 작업 대상은 `D:\codex\first-friend`와 `origin/main`뿐이다.
- 사용자가 명시적으로 배포를 요청하기 전에는 로컬에서만 작업한다. Vercel CLI나 임시 서버로 배포하지 않는다.
- 기존 사용자 변경사항과 다른 작업 세션의 변경사항을 보존한다.
- `.env`, `.env.local`, `.vercel`, 비밀키와 토큰을 커밋하지 않는다.
- `git reset`, `git checkout`, `git clean`, 강제 push를 사용하지 않는다.

## 디자인 시스템

- 당근 SEED Foundations와 설치된 SEED 컴포넌트·recipe·semantic token을 최우선으로 사용한다.
- 문구도 SEED Writing 기준을 따른다. 공식 문서: https://seed-design.io/foundations/writing
- SEED에 같은 목적의 컴포넌트가 있으면 커스텀 구현보다 SEED를 먼저 확인한다.
- SEED에 적절한 패턴이 없을 때만 `.codex-local/design-references/toss-tds`와 `docs/TOSS_TDS_REFERENCE.md`를 정보 구조·상태·행동 우선순위 참고용으로 사용한다.
- TDS의 색상·타이포그래피·간격·radius·elevation·아이콘 토큰을 SEED와 임의로 섞지 않는다.

## SEED Writing 규칙

모든 화면 문구, 버튼, 라벨, 안내, 오류, 알림, 빈 상태, AI 결과 문구에 적용한다.

- 익숙한 생활 언어를 사용하고 기술 용어·한자어·내부 용어를 사용자 말로 바꾼다.
- 존칭어를 최소화한다. 퍼스트프렌드의 따뜻하고 담백한 `~해요` 말투를 기본으로 한다.
- 문장 하나에는 한 가지 목적만 담고, 결론과 사용자가 할 일을 먼저 쓴다.
- 기능 이름보다 사용자가 하거나 얻는 목적을 쓴다. 예: `채팅하기`보다 `보호처와 상담하기`.
- 버튼은 짧은 동사형으로 쓴다. 예: `시작하기`, `다시 확인하기`, `신고 접수`.
- 오류 문구는 문제, 필요한 조치, 다음 방법 순서로 안내하고 사용자를 탓하지 않는다.
- 같은 의미라면 긍정문과 능동문을 우선한다. `저장할 수 없어요`보다 가능한 경우 `저장하려면 내용을 확인해 주세요`처럼 쓴다.
- 숫자는 아라비아 숫자로 쓴다. 축약어·은어·속어·유행어는 사용하지 않는다.
- 기능명·서비스명·상태값 자체는 공식 표기를 유지하고, 일반 문장에서는 자연스럽게 띄어 쓴다. 예: 상태값 `예약중`, 문장 `예약 중일 때`.
- 느낌표는 꼭 필요한 순간에만 1개 사용한다.
- 평서문·명령문에는 마침표를 사용한다. 메인 타이틀, 큰 글씨, 버튼·메뉴·라벨에는 생략할 수 있다.
- 지역사회와 보호자의 안전을 고려하되, 동물을 불쌍하게 묘사하거나 건강·성격·입양 가능성을 추측해 단정하지 않는다.
- AI 추정은 추정이라고 밝히고, 건강·성격·입양 가능 여부는 보호처 확인이 필요하다고 안내한다.

## 작업 전후 확인

- 작업 전 현재 경로, remote, 브랜치, `git status`, 최근 커밋, 기존 변경사항을 확인한다.
- 수정은 `apply_patch`로 하고 의도한 파일만 변경한다.
- 수정 후 `npm run lint`와 `npm test`를 실행한다. 실패하면 커밋·push하지 않는다.
- 사용자가 배포를 요청한 경우에만 의도한 파일을 staging하고 커밋한 뒤 `origin main`에 push한다.
