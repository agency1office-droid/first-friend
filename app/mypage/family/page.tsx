import type{Metadata}from"next";import{desc,eq}from"drizzle-orm";import{requireChatGPTUser}from"../../chatgpt-auth";import{getDb}from"../../../db";import{familyRooms}from"../../../db/schema";import{List,ListDivider,ListLinkItem}from"seed-design/ui/list";import{IconChevronRightLine,IconPeople3Line}from"@karrotmarket/react-monochrome-icon";export const dynamic="force-dynamic";export const metadata:Metadata={title:"가족과 상의한 친구"};export default async function Page(){const user=await requireChatGPTUser("/mypage/family"),rows=await getDb().select().from(familyRooms).where(eq(familyRooms.ownerId,user.userId)).orderBy(desc(familyRooms.createdAt));return <div className="ff-page"><header className="ff-page-header"><div className="ff-kicker">가족과 함께 보기</div><h1 className="ff-title">상의 중인 친구</h1><p className="ff-description">찬성·질문·걱정을 함께 모아 입양 전 준비에 반영하세요.</p></header><List>{rows.map((row,index)=><div key={row.id}><ListLinkItem href={`/family/${row.shareToken}`} prefix={<IconPeople3Line/>} title={row.title} detail={`${row.status} · ${new Date(row.createdAt).toLocaleDateString("ko-KR")}`} suffix={<IconChevronRightLine/>}/>{index<rows.length-1&&<ListDivider/>}</div>)}</List>{!rows.length&&<div className="ff-empty">동물 상세에서 ‘질문·가족 상의’를 눌러 시작하세요.</div>}</div>}
