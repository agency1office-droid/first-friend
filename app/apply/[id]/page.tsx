import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { animalById } from "../../../lib/data"; import { ApplicationForm } from "../../components/ApplicationForm";
export const metadata:Metadata={title:"입양 신청"};
export default async function ApplyPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const animal=animalById(id);if(!animal)notFound();return <div className="page"><span className="eyebrow">Safe adoption</span><h1 className="page-title">{animal.name}에게<br />마음을 전할게요.</h1><p className="page-subtitle">신청 순서가 입양을 결정하지 않습니다. 준비도는 상담을 돕는 참고 정보이며 최종 결정은 보호자가 합니다.</p><div className="timeline"><div className="timeline-step"><span className="timeline-dot">1</span><div className="timeline-content"><strong>신청서와 준비도 확인</strong><span>모든 신청자를 빠짐없이 확인해요.</span></div></div><div className="timeline-step"><span className="timeline-dot">2</span><div className="timeline-content"><strong>플랫폼 안에서 상담</strong><span>연락처와 정확한 주소를 공개하지 않아요.</span></div></div><div className="timeline-step"><span className="timeline-dot">3</span><div className="timeline-content"><strong>안전 동의와 인계</strong><span>양쪽이 확인한 뒤 가족이 됩니다.</span></div></div></div><ApplicationForm animalId={animal.id} animalName={animal.name}/></div>}
