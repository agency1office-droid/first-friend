import { getSupabaseServerClient } from "../../../../lib/supabase/server";import { clean } from "../../_helpers";
export async function GET(_:Request,{params}:{params:Promise<{token:string}>}){const{token}=await params;const client=getSupabaseServerClient(),{data:rooms}=await client.from("family_rooms").select("*").eq("share_token",token).limit(1),room=rooms?.[0];if(!room)return Response.json({error:"상의방을 찾을 수 없습니다."},{status:404});const{data:opinions}=await client.from("family_opinions").select("*").eq("room_id",room.id).order("created_at");return Response.json({room,opinions:opinions||[]});}
export async function POST(request:Request,{params}:{params:Promise<{token:string}>}){const{token}=await params;const client=getSupabaseServerClient(),{data:rooms}=await client.from("family_rooms").select("id").eq("share_token",token).limit(1),room=rooms?.[0];if(!room)return Response.json({error:"상의방을 찾을 수 없습니다."},{status:404});const data=await request.json()as Record<string,unknown>,authorName=clean(data.authorName,30),body=clean(data.body,500),decision=clean(data.decision,20);if(!authorName||body.length<2||!["agree","question","concern"].includes(decision))return Response.json({error:"이름·의견·선택을 확인해 주세요."},{status:400});const{data:opinion,error}=await client.from("family_opinions").insert({room_id:room.id,author_name:authorName,body,decision}).select("*").single();if(error)return Response.json({error:"의견을 저장하지 못했어요."},{status:500});return Response.json({opinion},{status:201});}
