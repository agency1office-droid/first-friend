"use client";import{useState}from"react";import{ActionButton}from"seed-design/ui/action-button";import{TextField,TextFieldInput,TextFieldTextarea}from"seed-design/ui/text-field";import{Callout}from"seed-design/ui/callout";
export function LostFoundForm(){const[kind,setKind]=useState("lost"),[done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/lost-found",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind,species:f.get("species"),region:f.get("region"),occurredAt:f.get("occurredAt"),description:f.get("description")})});if(r.status===401){window.location.href="/signin-with-chatgpt?return_to=%2Flost-found";return}if(r.ok)setDone(true)}if(done)return <div className="ff-result"><h2 className="ff-section-title">{kind==="lost"?"실종 신고":"발견 제보"}를 접수했어요</h2><p className="ff-description" style={{marginTop:8}}>정확한 위치와 연락처는 공개되지 않아요.</p></div>;return <form className="ff-form" onSubmit={submit}><div className="ff-radio-grid"><label className="ff-radio-card"><input type="radio" checked={kind==="lost"} onChange={()=>setKind("lost")}/> 반려동물을 찾고 있어요</label><label className="ff-radio-card"><input type="radio" checked={kind==="found"} onChange={()=>setKind("found")}/> 동물을 발견했어요</label></div><div className="ff-field"><label htmlFor="species">동물 종류</label><select className="ff-native-select" id="species" name="species"><option>고양이</option><option>강아지</option><option>새</option><option>기타</option></select></div><TextField label="대략적인 지역" required><TextFieldInput name="region" placeholder="예: 서울 마포구" required/></TextField><TextField label="목격 또는 발견 시각" required><TextFieldInput name="occurredAt" type="datetime-local" required/></TextField><TextField label="외형과 상황" required><TextFieldTextarea name="description" minLength={20} required/></TextField><Callout tone="critical" description="발견한 동물을 무리하게 쫓거나 포획하지 마세요. 위험한 상황에서는 관계기관에 먼저 도움을 요청하세요."/><ActionButton size="large" className="ff-action-link">본인 확인 후 등록하기</ActionButton></form>}
