import { env } from "cloudflare:workers";
import { authenticatedDb } from "../../_helpers";
export async function GET(request: Request) { const auth = await authenticatedDb(); if (!auth || auth.member.role !== "admin") return new Response("Forbidden", { status: 403 }); const key = new URL(request.url).searchParams.get("key") || ""; if (!(key.startsWith("private-evidence/") || key.startsWith("appeal-evidence/")) || key.includes("..")) return new Response("Not found", { status: 404 }); const object = await env.MEDIA.get(key); if (!object) return new Response("Not found", { status: 404 }); const headers = new Headers(); object.writeHttpMetadata(headers); headers.set("cache-control", "private, no-store"); headers.set("content-disposition", "inline"); headers.set("x-content-type-options", "nosniff"); return new Response(object.body, { headers }); }
